export declare enum ProjectType {
    app = "app",
    lib = "lib",
}
export declare enum DependencyType {
    es6Import = "es6Import",
    loadChildren = "loadChildren",
}
export declare type ProjectNode = {
    name: string;
    root: string;
    type: ProjectType;
    tags: string[];
    files: string[];
};
export declare type Dependency = {
    projectName: string;
    type: DependencyType;
};
export declare function touchedProjects(projects: ProjectNode[], touchedFiles: string[]): string[];
export declare function affectedApps(npmScope: string, projects: ProjectNode[], fileRead: (s: string) => string, touchedFiles: string[]): string[];
export declare function dependencies(npmScope: string, projects: ProjectNode[], fileRead: (s: string) => string): {
    [projectName: string]: Dependency[];
};
